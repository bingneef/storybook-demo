{
  "name": "test",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react-scripts": "2.1.1"
  },
  "scripts": {
    "test:integration": "jest -c __tests__/e2e/jest.config.js",
    "docker:test:integration": "yarn wait-on -l http-get://mock_build:5000 && APP_URL=http://mock_build:5000 yarn test:integration"
  },
  "devDependencies": {
    "jest-puppeteer": "^3.6.0",
    "puppeteer": "^1.11.0",
    "wait-on": "^3.2.0"
  }
}